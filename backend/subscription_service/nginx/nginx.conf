upstream subscription_service_servers {
    server subscription_service:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://subscription_service_servers;
        
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}